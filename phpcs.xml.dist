<?xml version="1.0"?>
<ruleset name="PHP SimpleQueue Coding Standards">
    <description>PSR-12 with stricter quality rules and Slevomat sniffs</description>

    <arg name="colors"/>
    <arg name="parallel" value="80"/>
    <arg name="extensions" value="php"/>

    <file>src</file>
    <file>tests</file>
    <file>benchmarks</file>
    <file>scripts</file>
    <file>examples</file>

    <!-- Base ruleset -->
    <rule ref="PSR12"/>

    <!-- Exclude rules for tests -->
    <!-- Tests can define multiple classes/interfaces in a single file for mocks -->
    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude-pattern>benchmarks/*</exclude-pattern>
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>
    <!-- Tests can have side effects (e.g. bootstrapping, mock definitions) -->
    <rule ref="PSR1.Files.SideEffects.FoundWithSymbols">
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude-pattern>benchmarks/*</exclude-pattern>
        <exclude-pattern>scripts/*</exclude-pattern>
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>
    <!-- Executable examples and benchmark fixtures intentionally use the global namespace. -->
    <rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">
        <exclude-pattern>benchmarks/*</exclude-pattern>
        <exclude-pattern>scripts/*</exclude-pattern>
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>
    <!-- Tests can have long lines (assertions, mock calls, SQL strings) -->
    <rule ref="Generic.Files.LineLength">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <!-- Line length limits -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="180"/>
        </properties>
    </rule>

    <!-- Code complexity / Nesting limits -->
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="nestingLevel" value="3"/>
            <property name="absoluteNestingLevel" value="5"/>
        </properties>
    </rule>
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="15"/>
            <property name="absoluteComplexity" value="25"/>
        </properties>
    </rule>

    <!-- Maintainable production-code size limits. -->
    <rule ref="SlevomatCodingStandard.Functions.FunctionLength">
        <properties>
            <property name="maxLinesLength" value="100"/>
        </properties>
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude-pattern>benchmarks/*</exclude-pattern>
        <exclude-pattern>scripts/*</exclude-pattern>
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>
    <rule ref="SlevomatCodingStandard.Classes.ClassLength">
        <properties>
            <property name="maxLinesLength" value="500"/>
        </properties>
        <!-- Retained compatibility surface prevents a safe v1 split. -->
        <exclude-pattern>src/Storage/PdoJobStorage.php</exclude-pattern>
        <exclude-pattern>tests/*</exclude-pattern>
        <exclude-pattern>benchmarks/*</exclude-pattern>
        <exclude-pattern>scripts/*</exclude-pattern>
        <exclude-pattern>examples/*</exclude-pattern>
    </rule>

    <!-- Slevomat Coding Standard Sniffs -->
    <!-- Enforce declare(strict_types=1) -->
    <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
        <properties>
            <property name="spacesCountAroundEqualsSign" value="0"/>
        </properties>
    </rule>

    <!-- Enforce Type Hints (exclude tests to avoid overly verbose mock definitions) -->
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHint">
        <exclude-pattern>tests/*</exclude-pattern>
        <!-- Predis v3 retains untyped interface parameters; the benchmark adapter must match them. -->
        <exclude-pattern>benchmarks/redis-instrumentation.php</exclude-pattern>
    </rule>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <!-- Enforce Constructor Property Promotion -->
    <rule ref="SlevomatCodingStandard.Classes.RequireConstructorPropertyPromotion"/>

    <!-- Remove unused imports -->
    <rule ref="SlevomatCodingStandard.Namespaces.UnusedUses"/>

    <!-- Detect dead catch blocks -->
    <rule ref="SlevomatCodingStandard.Exceptions.DeadCatch"/>

    <!-- Detect useless variables -->
    <rule ref="SlevomatCodingStandard.Variables.UselessVariable"/>

    <!-- Detect useless ternaries -->
    <rule ref="SlevomatCodingStandard.ControlStructures.UselessTernaryOperator"/>
</ruleset>
